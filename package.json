{
  "name": "sentio",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.34.3",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "build": "pnpm -r --if-present run build",
    "functions:sync": "node scripts/sync-shared-to-functions.mjs",
    "verify:frontieres": "node scripts/verifier-frontieres.mjs",
    "functions:verify": "pnpm run functions:sync && deno lint --config supabase/functions/deno.json supabase/functions/diagnostic supabase/functions/_shared && deno check --config supabase/functions/deno.json supabase/functions/diagnostic/index.ts && deno test --config supabase/functions/deno.json --allow-env supabase/functions/diagnostic/",
    "functions:deploy": "pnpm run functions:verify && supabase functions deploy diagnostic",
    "verify": "pnpm run lint && pnpm run verify:frontieres && pnpm run typecheck && pnpm run test && pnpm run build && pnpm run functions:verify",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.18.0",
    "@typescript-eslint/parser": "^8.18.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
