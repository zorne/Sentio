node_modules/
dist/
*.tsbuildinfo

# Secrets — aucun ne doit entrer dans le dépôt (AGENTS.md, invariant 7).
.env
.env.*
!.env.example

# Interface : sortie de construction et fichiers engendrés par Next.
.next/
build/

# Outillage de conception installé localement (skills UI/UX, 2,8 Mo de données tierces).
# Régénérable en une commande — voir README, « Outillage local ». Le produit n'en dépend pas.
.claude/
